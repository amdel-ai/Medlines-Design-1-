# Medlines — Design Draft 1

Responsive, human-verified medical transcription website built with Next.js.

## Local development

```bash
npm install
npm run dev
```

## Vercel

Import the GitHub repository into Vercel, choose **Next.js** as the framework,
and leave the build and output directory overrides disabled.
