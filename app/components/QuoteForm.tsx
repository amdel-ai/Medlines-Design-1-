"use client";
import type { FormEvent } from "react";
export function QuoteForm() {
  const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = new FormData(event.currentTarget); const subject = encodeURIComponent("Medlines Transcription quote / free trial request"); const body = encodeURIComponent(`Name: ${data.get("name")}\nPractice / Business: ${data.get("practice")}\nEmail: ${data.get("email")}\nPhone: ${data.get("phone")}\nService needed: ${data.get("service")}\n\n${data.get("message")}`); window.location.href = `mailto:service@medlinesmtb.com?subject=${subject}&body=${body}`; };
  return <form className="quote-form" id="quote-form" onSubmit={submit}><div className="form-heading"><span>Let&apos;s get started</span><strong>Tell us about your practice.</strong></div><div className="form-row"><label><span>Name</span><input name="name" type="text" placeholder="Jane Smith" autoComplete="name" required /></label><label><span>Practice / Business Name</span><input name="practice" type="text" placeholder="Your practice" autoComplete="organization" /></label></div><div className="form-row"><label><span>Email</span><input name="email" type="email" placeholder="jane@practice.com" autoComplete="email" required /></label><label><span>Phone</span><input name="phone" type="tel" placeholder="(555) 000-0000" autoComplete="tel" /></label></div><label><span>Service Needed</span><select name="service" defaultValue="Medical Transcription"><option>Medical Transcription</option><option>Medicolegal Transcription</option><option>Transcription Technology</option></select></label><label><span>Message</span><textarea name="message" rows={3} placeholder="Tell us a little about your transcription needs." /></label><div className="form-actions"><button className="button button-light" type="submit">Request a Quote <span>↗</span></button><a className="button button-glass" href="mailto:service@medlinesmtb.com?subject=Start%20My%20Free%20Trial">Free Trial</a></div></form>;
}
